# Порт, на котором запускается сервер
PORT=4000

# Строка подключения к MongoDB
MONGODB_URI=mongodb://127.0.0.1:27017/miraziz

# Разрешённые источники для CORS (через запятую). * — разрешить всем
CORS_ORIGIN=*
